import { name as typeName } from "../types/ucs2-string";
function write(type, value) {
    return value;
}
function read(type, input) {
    const error = type.testError(input);
    if (error !== undefined) {
        throw error;
    }
    return input;
}
function readTrusted(type, input) {
    return input;
}
export const SERIALIZER = {
    typeName,
    write,
    read,
    readTrusted,
};
export function register(serializer) {
    serializer.register(SERIALIZER);
}
