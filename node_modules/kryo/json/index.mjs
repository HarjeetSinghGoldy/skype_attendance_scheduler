import { Incident } from "incident";
export const JSON_SERIALIZER = {
    format: "json",
    register(serializer) {
        throw new Incident("NotImplemented", "JSON_SERIALIZER.register");
    },
    write(type, value) {
        return type.writeJson(value);
    },
    read(type, input) {
        return type.readJson(input);
    },
    readTrusted(type, input) {
        return type.readTrustedJson(input);
    },
};
